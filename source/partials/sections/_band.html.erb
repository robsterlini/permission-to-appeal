<h2 class="visuallyhidden"><%= s.title %></h2>
<!--<% data.band.band.each_with_index do |member, index| %>
  --><div class="band__member" style="background-image: url(<%= "#{images_dir}/faces/#{member.name.to_slug}-hero.jpg" %>)">
    <div class="group">
      <%= image_tag "faces/#{member.name.to_slug}.jpg", :title => "A photo of #{member.name}", :class => "band__member__face" %>
      <h3><%= member.nickname || member.name %></h3>
      <% member.copy.each do |text| %>
        <p><%= text %></p>
      <% end %>
    </div>
  </div><!--
<% end %>-->
<div class="band__member band__member--alumni">
  <div class="group">
    <h2><%= s.data.alumni.title %></h2>
    <% s.data.alumni.copy.each do |text| %>
      <p><%= text %></p>
    <% end %>
    <ul class="band__alumni">
      <!--<% data.band.alumni.each_with_index do |alumnus, index| %>
        --><li class="band__alumnus">
          <%= image_tag "faces/#{alumnus.name.to_slug}.jpg", :title => "A photo of #{alumnus.name}", :class => "band__member__face" %>
          <strong class="band__alumnus__name"><%= "#{alumnus.name}" %></strong>
        </li><!--
      <% end %>-->
    </ul>
  </div>
</div>