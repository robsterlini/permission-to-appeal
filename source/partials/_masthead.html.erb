 <header class="masthead">
  <div class="group"> 
    <%= link_to default_site_title, "index.html", :title => "Back to the homepage", :class => "masthead__logo link--reversed" %>
    <nav class="nav">
      <ul class="nav__list"><!--
        <% data.home.sections.each do |nav| %>
          <% if nav.masthead %>
            --><li class="nav__item">
              <%= link_to "#{nav.masthead_title || nav.title}", "##{nav.slug || nav.partial}", :class => "nav__link--#{nav.slug || nav.partial} link--reversed" %>
            </li><!--
          <% end %>
        <% end %>
      --></ul>
    </nav>
    <div class="btns--sizing">
      <%= link_to "Book us", "#contact", :class => "btn" %>
    </div>
  </div>
</header>